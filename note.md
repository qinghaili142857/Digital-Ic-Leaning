# <center>超标量处理器设计学习笔记

乱序执行：
fetch -> decode -> register renaming -> dispatch(分发) -> issue -> register file read -> execute -> write back -> commit

## Cache
### 2.1 Cache的一般设计
cache：tag+data
用一个更直观的类比——**图书馆**，

---

### 1. 先明确一个问题：Cache 要解决什么？
CPU 要读取内存中的数据，但内存很大、很慢。  
Cache 是一个小而快的中转区，存着最近常用的数据。  
问题是：**CPU 给了一个内存地址，怎么在 Cache 里快速判断这个数据在不在？如果在了，又放在哪里？**

Cache 把内存地址拆成三部分来做到这一点：**Tag + Index + Offset**  
而 Byte 和 Word 与 Offset 有关。

---

### 2. 用图书馆类比

想象一个 **图书馆（内存）** 有无数本书。  
你（CPU）想找一本特定的书。  
图书馆里有一个 **小型快速检索柜（Cache）**，里面只能放几十本书。

**这个检索柜有很多格子（Cache 行），每个格子放一本书。**

- **Index**：格子的**编号**  
  （比如第 3 排、第 5 格）

- **Tag**：书的**唯一标识**  
  （比如书的 ISBN 后几位，用来确认格子里放的是不是你想要的那本）

- **Offset**：如果一本书很厚，里面分很多页。  
  你找到书之后，还要翻到具体哪一页。  
  对应到 Cache 里，就是：**在一个 Cache 行（格子）里，定位到具体的字节或字**。

---

### 3. 逐个解释

#### （1）Byte（字节）
- 在 Cache 里：  
  当你找到数据所在的那一行（64 字节），你需要知道**取这 64 个字节里的第几个字节**。  
  Byte 就是最小的定位单位。

#### （2）Word（字）
- 在 Cache 里：  
  CPU 通常一次读 4 或 8 个字节（一个字）。  
  所以在 64 字节的行里，它会先定位到**第几个字**，再在这个字里定位到字节。  
  可以理解为：**Word 是比 Byte 更大一步的偏移**。

#### （3）Index（索引）
- 在 Cache 里：  
  **决定数据放在哪个“组”或“格子”**。  
  因为 Cache 比较小，不能用整个地址去满世界找，所以用地址的中间几位直接算出格子号。  
  就像：你要找的书，先根据一个规则确定它只能放在 **第 5 号格子**，不用翻遍所有格子。

#### （4）Tag（标签）
- 在 Cache 里：  
  虽然通过 Index 找到了格子，但格子里的书可能不是你要的那本（因为不同书可能被算到同一个格子）。  
  **Tag 就是用来确认：这个格子里存的，到底是不是你当前要找的数据**。  
  如果 Tag 匹配 → **命中**  
  如果不匹配 → **未命中**，要去内存取。

---

### 4. 用地址拆分解释得更直观

假设一个内存地址是 32 位的二进制数，Cache 设计时把它切成三段：

```
[   Tag   ] [ Index ] [ Offset ]
```

- **Offset**（低几位）  
  用来在一行里选具体的字节或字。  
  比如一行 64 字节，就需要 6 位（2⁶=64）。

- **Index**（中间几位）  
  用来选“第几组”。  
  比如有 512 组，就需要 9 位。

- **Tag**（高位剩下的位）  
  用来区分同一个 Index 下，不同内存地址的数据。

---

### 5. 一次完整查找流程（类比）

1. CPU 给出地址 `0x1A3F`。
2. 提取 **Index** → 去 Cache 的第 5 组。
3. 这一组里可能有 8 个格子（8 路组相联），挨个检查每个格子的 **Tag**。
4. 如果某个格子的 Tag 和地址里的 Tag 一样 → **命中**，数据在这个格子里。
5. 再用 **Offset** 从这个格子的 64 字节里取出想要的字节或字。

---

### 6. 为什么这样设计？
- **Index**：让查找范围从“整个 Cache”缩小到“某几个格子”，硬件简单、速度快。
- **Tag**：解决“不同地址可能映射到同一个格子”的问题，保证准确性。
- **Offset**：因为 Cache 一次拿一整块数据（Cache 行），但 CPU 可能只取其中一小部分。

如果你愿意，我可以画一个 **地址拆分 + 查找过程的简单图示**，会比纯文字更清楚。你也可以告诉我，上面哪一段还是觉得模糊，我再单独展开讲。

直接映射、组相连、全相连

cache miss：3C定理
1.compulsory
2.capcity
3.conflict

solution：prefetching、victim cache

并行访问 流水线 TagSRAM DataSRAM